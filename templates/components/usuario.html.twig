    {# <div class="alert">
        <hr>
        Usuario: {{ user }} |    Rol: {{ rol }} |  <a href="/" >Home</a> |  <a href="/logout" >Cerrar sesión</a>
        <hr>
        style="background-color:rgb(#96CD7A);"
    </div> #}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid" style="background-color:#96CD7A;">
    <img  alt="" width="50" height="50" class="d-inline-block align-text-top" src="{{ asset('build/img/logo2.png') }}" >
    <a class="navbar-brand" >VacunasSist</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {# <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Features</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li> #}
      </ul>
      <span class="navbar-text">
        {% if env == 'dev' %}
           <i class="fa-solid fa-user"></i> Usuario: {{ user }} |    Rol: {{ rol }} |  <a href="/" ><i class="fa-solid fa-house-user"></i></a> |  <a href="/logout" ><i class="fa-solid fa-right-from-bracket"></i></a>
		{% else %}


            {% if rol == 'P' %}
               <i class="fa-solid fa-user"></i> Paciente: {{ user }} |  <a href="{{ homePage }}" ><i class="fa-solid fa-house-user"></i></a> | <i id="bell" class="fa-solid fa-bell" ></i> |  <a href="/logout" ><i class="fa-solid fa-right-from-bracket"></i></a>
            {% endif %}
            {% if rol == 'V' %}
               <i class="fa-solid fa-user"></i> Vacunador: {{ user }} |  <a href="{{ homePage }}" ><i class="fa-solid fa-house-user"></i></a> |  <a href="/logout" ><i class="fa-solid fa-right-from-bracket"></i></a>
            {% endif %}
            {% if rol == 'A' %}
               <i class="fa-solid fa-user"></i> Admin: {{ user }} |  <a href="{{ homePage }}" ><i class="fa-solid fa-house-user"></i></a> |  <a href="/logout" ><i class="fa-solid fa-right-from-bracket"></i></a>
            {% endif %}
            {% if rol == 'x' %}
                <a href="/custom/login/paciente" >Iniciar Sesión</a>
            {% endif %}


        {% endif %}

      </span>
    </div>
  </div>
</nav>